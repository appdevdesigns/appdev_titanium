// Initialize the AppDev framework
var AD = require('AppDev');
AD.init({
    models: [], // array of all the models used by the application
    windows: ['AppInfoWindow'] // array of all the tab bar windows used by the application
});
